page('/', homeController.reveal);
page('/results', resultsController.reveal);
page('/results+stats', /*Add Results and Extra Info Controller later*/);
page('/about', aboutController.reveal);

page();
